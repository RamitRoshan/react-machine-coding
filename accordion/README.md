 # Material UI Icons 

**Installation:**

```
npm install @mui/icons-material @mui/material @emotion/styled @emotion/react
```

- For use, we have to import it in the particular component, wherever You wants to use , Once import it then , using tags we can use it like this 👇🏻

```
import './App.css'
import Brightness3Icon from '@mui/icons-material/Brightness3';

function App() { 

  return (
    <div>
      <h2>Accordion</h2>
      <Brightness3Icon/>
    </div>
  )
}

export default App
```